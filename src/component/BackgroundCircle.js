import React from "react";

export const BackgroundCircle = ({radius, strokeWidth}) => (
      <circle
          r={radius}
          fill="#F7F700"
          stroke="black"
          stroke-width={strokeWidth}
        />
    )
